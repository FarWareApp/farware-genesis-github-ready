import express from 'express';import fs from 'fs';import path from 'path';const router=express.Router();router.post('/',(req,res)=>{const {filename}=req.body||{};if(!filename)return res.status(400).json({error:'لم يتم تحديد اسم الملف'});const p=path.join(process.cwd(),'uploads',filename);if(!fs.existsSync(p))return res.status(404).json({error:'الملف غير موجود'});res.json({message:'✅ الفحص ناجح: الملف موجود',file:filename});});export default router;