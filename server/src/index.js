import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import dotenv from 'dotenv';import uploadRouter from './routes/upload.js';import validateRouter from './routes/validate.js';import buildRouter from './routes/build.js';dotenv.config();const app=express();const PORT=process.env.PORT||8080;const ORIGIN=process.env.FRONTEND_ORIGIN||'*';app.use(helmet());app.use(cors({origin:ORIGIN}));app.use(express.json());app.use(morgan('dev'));app.get('/',(req,res)=>res.json({message:'FarWare Genesis API ✅',origin:ORIGIN}));app.use('/api/upload',uploadRouter);app.use('/api/validate',validateRouter);app.use('/api/build',buildRouter);app.listen(PORT,()=>console.log(`🚀 API http://localhost:${PORT}`));